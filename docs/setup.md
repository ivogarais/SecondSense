# Setup

1. Open this repo in Android Studio once.
2. Let Android Studio sync and generate `gradlew`, `gradlew.bat`, and `gradle/wrapper/gradle-wrapper.jar`.
3. Return to VS Code and continue coding.
